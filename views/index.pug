extends layout

block content
  if isLoggedIn
    div(class="wrapper")
      div(class="header")
        div(class="jumbotron jumbotron-fluid")
          div(class="container")
            h1(class="display-3") #{title}
            p(class="lead") Welcome to #{title}
  else 
    div(class="wrapper")
      div(class="header")
        div(class="jumbotron jumbotron-fluid")
          div(class="container")
            h1(class="display-3") #{title}
            p(class="lead") Welcome to #{title}
      div(class="jumbotron jumbotron-fluid")
        div(class="container")
          h1(class="display-3") #{title}
          p(class="lead") Welcome to #{title}
      div(class="jumbotron jumbotron-fluid")
        div(class="container")
          h1(class="display-3") #{title}
          p(class="lead") Welcome to #{title}
      div(class="jumbotron jumbotron-fluid")
        div(class="container")
          h1(class="display-3") #{title}
          p(class="lead") Welcome to #{title}
      div(class="jumbotron jumbotron-fluid")
        div(class="container")
          h1(class="display-3") #{title}
          p(class="lead") Welcome to #{title}